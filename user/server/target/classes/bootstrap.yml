spring:
  application:
    name: user
  cloud:
    config:
      profile: dev
      discovery:
        enabled: true
        service-id: config

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/